<Window x:Class="Memory_game.View.ServerListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Memory_game.View"
        xmlns:vm ="clr-namespace:Memory_game.ViewModel"
        mc:Ignorable="d"
        Title="ServerListWindow" Height="450" Width="800"
        d:DataContext="{d:DesignInstance Type=vm:ServerListWindowViewModel}">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <ListBox ItemsSource="{Binding AvailableServers}"
                 SelectedItem="{Binding SelectedServer}"/>
        <Button Content="Connect" Grid.Row="1" Width="100" Height="50"
                Command="{Binding ConnectToSevrer}"/>

    </Grid>
</Window>
