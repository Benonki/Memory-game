<Window x:Class="Memory_game.View.BoardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Memory_game.View"
        xmlns:vm="clr-namespace:Memory_game.ViewModel"
        mc:Ignorable="d"
        Title="Plansza gry" Height="800" Width="800" MinHeight="450" MinWidth="450"
        d:DataContext="{d:DesignInstance Type=vm:BoardWindowViewModel}">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <Style x:Key="CardButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Background" Value="LightGray"/>
            <Setter Property="BorderBrush" Value="DarkGray"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="Gray"/>
                    <Setter Property="Opacity" Value="0.6"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="LightGray" Padding="10" Margin="5">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <TextBlock Text="Mój wynik: " FontSize="18" FontWeight="Bold"/>
                    <TextBlock Text="{Binding MyScore}" FontSize="18" FontWeight="Bold" Foreground="Green"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="{Binding CurrentTurnText}" FontSize="18" FontWeight="Bold"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="Wynik przeciwnika: " FontSize="18" FontWeight="Bold"/>
                    <TextBlock Text="{Binding OpponentBestScore}" FontSize="18" FontWeight="Bold" Foreground="Red"/>
                </StackPanel>
            </Grid>
        </Border>

        <ProgressBar Grid.Row="1" Height="10" Margin="10,5" 
                     Value="{Binding TimeLeft}" Maximum="{Binding TurnTimeSeconds}" 
                     Visibility="{Binding CanInteract, Converter={StaticResource BooleanToVisibilityConverter}}"/>

        <Grid Grid.Row="2" Margin="10">
            <ItemsControl ItemsSource="{Binding Cards}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Rows="{Binding Rows}" Columns="{Binding Columns}"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button 
                            Command="{Binding DataContext.FlipCardCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                            CommandParameter="{Binding}"
                            Style="{StaticResource CardButtonStyle}"
                            IsEnabled="{Binding DataContext.CanInteract, RelativeSource={RelativeSource AncestorType=Window}}">
                            <Grid>
                                <Image Source="{Binding ImagePath}" 
                                       Visibility="{Binding IsFaceUp, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                            </Grid>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Grid>
</Window>